# Plain Protest — Brand Kit v1.3 (ultra-minimal)

**Change requested:** remove everything below the CTA; show only a tiny, artful step strip at the very bottom.

## What’s in this folder
- `brand_tokens.css` — same refined tokens (narrow measure, soft ink)
- `components.css` — adds body flex layout + minimal steps footer
- `index.html` — header + hero + CTA + bottom step strip (nothing else)
- `favicon.ico` & icons + `og-image.png`

## Notes
- The bottom step strip stays pinned to the bottom on short pages (flex layout).
- Keep one primary CTA per screen. Use the accent only for CTAs/links.
- To change the three lines, edit the `.steps` content in `index.html`.
